type: google.api.Service
config_version: 3

http:
  rules: 
  - selector: protocol.chatService.login
    post: /v1/chat/login
  - selector: protocol.chatService.logout
    post: /v1/chat/logout
  - selector: protocol.chatService.subscribe
    post: /v1/chat/subscribe
